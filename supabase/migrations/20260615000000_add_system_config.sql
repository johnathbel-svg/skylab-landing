CREATE TABLE public.system_config (key TEXT PRIMARY KEY, value JSONB NOT NULL, updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()); ALTER TABLE public.system_config ENABLE ROW LEVEL SECURITY; CREATE POLICY " Super admins can manage system config\ ON public.system_config USING (public.is_super_admin()) WITH CHECK (public.is_super_admin()); INSERT INTO public.system_config (key, value) VALUES ('whatsapp_gateway_enabled', 'true'::jsonb), ('web_widget_enabled', 'true'::jsonb);
